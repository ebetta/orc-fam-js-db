# A aplicação roda localmente via Express + PostgreSQL.
# Não há variáveis de ambiente obrigatórias hoje: a conexão com o Postgres é
# configurada no topo de server.js, e o frontend fala com http://localhost:3001/api.
